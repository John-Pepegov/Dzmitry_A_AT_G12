package homework.read_write;

public class ReadWrite8 {
    // читает какой-нибудь объект класса (добавить имплементацию интерфейса Serializable) из пакета playground.essence
    // из файла (например, записанный в предыдущем задании)
    // и записывает в другой текстовый файл результат возвращаемого значения метода toString() этого объекта
}
